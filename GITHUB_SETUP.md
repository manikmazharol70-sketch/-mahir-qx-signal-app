# GitHub setup

1. Create/open the GitHub repository.
2. Upload all files in this folder (not the ZIP itself if you want the repository to contain the source).
3. Commit with: `Add MaHiR QX AI Quotex Helper`.
4. In Chrome: `chrome://extensions` → Developer mode → Load unpacked → select this folder.

This build keeps trade execution behind manual confirmation.
